services:
  backend:
    container_name: backend
    image: backend
    networks:
      - app-network
  nginx:
    container_name: nginx
    image: nginx
    networks:
      - app-network
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf

networks:
  app-network:
    driver: bridge          
